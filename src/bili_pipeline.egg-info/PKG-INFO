Metadata-Version: 2.4
Name: bili-pipeline
Version: 0.1.0
Summary: Minimal runnable scaffold for Bilibili video pool discovery.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: aiohttp
Requires-Dist: bilibili-api-python
Requires-Dist: openpyxl>=3.1.5
Requires-Dist: oss2
Requires-Dist: pandas
Requires-Dist: streamlit
Requires-Dist: xlrd>=2.0.2

# Bilibili 采集方法对比（v1）

本文档只回答“怎么拿到数据、各方法的优缺点与适用场景”，不涉及具体代码实现。

---

## 0. 数据分层与总原则

- **结构化数据（可入库）**：视频元数据、作者画像、统计快照、评论/弹幕文本、抓取日志与质量指标  
- **大文件资产（只存引用）**：视频文件、音频文件、封面、字幕 JSON/转写文本、弹幕原始文件（XML/Protobuf）  
- **核心建议**：把“数据发现/遍历”和“资产下载”解耦；把“高频短期监控”和“低频长期面板”分开调度，避免全量高频导致风控与资源爆炸。

---

## 1) 视频集合（样本池）获取方法

### 方法 A：榜单/热门冷启动（推荐，用于发现高传播样本）

- **入口类型**：热门、排行榜、系列榜单、精选等（平台官方聚合）  
- **优点**：
  - 入口相对稳定，适合持续监控热点样本
  - 冷启动成本低，不需要先有作者或关键词
- **缺点**：
  - **样本偏置**明显（偏热门、偏头部、偏当期）
  - 长尾覆盖差，不适合构建“全站分区全量”式数据集
- **适用场景**：热度演化研究、爆款扩散研究、热点舆情研究、做“重点样本池”

### 方法 B：按分区扩样（可用，但通常做不到严格全量）

- **入口类型**：分区排行榜 + 分区搜索（时间切片）+ 分区页解析（兜底）  
- **优点**：
  - 能按研究分区组织样本（例如动画/游戏/知识/生活）
  - 易于控制采样范围与主题一致性
- **缺点**：
  - **严格“分区全量枚举”入口不稳定/不直观**，需要组合多个入口才能近似覆盖
  - 搜索/列表排序策略会导致漏数与不可控截断
- **适用场景**：围绕 1–3 个研究分区构建可复现的“近全量样本”

### 方法 C：按作者投稿列表扩展（推荐，用于构建传播链或作者画像）

- **入口类型**：作者投稿列表（空间投稿）  
- **优点**：
  - 分页与排序更清晰，**非常适合“单作者全量遍历”**
  - 便于构建作者级统计、作者内容策略研究
- **缺点**：
  - 需要先知道 `uid(mid)`；属于“已知作者视角”，不适合冷启动发现
- **适用场景**：对入池视频的作者做扩展采样；构建作者传播链样本

### 方法 D：关键词/事件搜索（补充扩样，不建议做主链路）

- **入口类型**：按关键词、时间窗、分区过滤的搜索列表  
- **优点**：专题研究强（事件、热点、主题），可用时间切片控制样本的时间跨度  
- **缺点**：召回与排序不可控、可能截断；难以证明“全量”  
- **适用场景**：论文中的专题实验组/对照组扩样

### 方法 E：推荐/相关视频（不推荐做主数据源）

- **优点**：冷启动发现相似内容快  
- **缺点**：个性化强、可复现性差、易改版  
- **适用场景**：探索性分析、小规模补样

---

## 2) 流行度时间序列数据获取方法

你要的是“短期两小时一次 + 长期一天一次”的**累计值快照序列**。主方法是重复抓同一组字段快照并落表。

### 方法 A：统计快照（推荐，成本最低、最稳定）

- **可得字段**（累计值）：播放/点赞/投币/收藏/转发/评论/弹幕（以及你文档里的其他 stat 字段）  
- **优点**：
  - 抓取成本低，适合大规模长期面板
  - 便于构建 \( \Delta \) 增量、增长率、加速度等派生特征
- **缺点**：
  - 只能看到“累计值变化”，看不到真实事件流（何时发生、谁触发等）
  - 平台计数可能有延迟/回滚，增量会出现偶发负增长或跳变
- **适用场景**：全量日更 + 重点样本 2 小时级监控

### 方法 B：前 n 条评论快照（你提出的“独创”特征，建议定义为“快照”而非“真实前 n 条”）

- **定义建议**：
  - 明确“前 n 条”的排序：**热度序** 或 **时间序**（两者意义不同）
  - 固化：每次抓取记录 `snapshot_time` + `sort_mode` + `n`，并保存“本次快照拿到的评论集合”与其点赞数
- **优点**：能刻画“评论区最可见内容”的文本语义变化与互动强度  
- **缺点**：
  - 评论会被删除/折叠/审核，排序会随时间变化，导致快照漂移
  - 深翻页与楼中楼会显著放大请求量与风控风险
- **适用场景**：重点样本的舆情/互动演化；不建议对全量视频做 2 小时级评论快照

### 方法 C：弹幕时间序列（谨慎；建议先做“弹幕总数快照”，弹幕内容后做抽样）

- **优点**：弹幕密度与峰值往往能解释热度波动  
- **缺点**：弹幕内容获取是分段二进制（Protobuf/XML），全量保存成本高  
- **适用场景**：先存“弹幕总数快照”，内容侧用抽样或对重点视频做全量

---

## 3) 元数据获取方法

### 方法 A：视频详情接口（推荐主表来源）

- **覆盖字段**：标题/简介/封面/分区/发布时间/作者信息/分 P 列表入口/核心 stat 等  
- **优点**：字段最集中、稳定性相对高  
- **缺点**：标签、字幕等通常需要额外接口补齐  
- **适用场景**：`VideoMeta` 主表；每个视频至少抓一次

### 方法 B：标签/分 P / cid / 字幕入口补齐（按需抓）

- **用途**：
  - `cid` 是弹幕、字幕、分 P 资产关联的关键键
  - 字幕是文本模态的重要来源（若有）
- **建议**：把它们作为“多模态下载的前置字段”一起落表，便于后续下载/回补

### 方法 C：作者画像接口（作者表）

- **覆盖字段**：粉丝数、投稿数、作者侧统计等（偏作者级聚合）  
- **建议**：作者画像用独立表，和视频表通过 `owner_mid` 关联；并支持作者画像的时间序列（例如日更）

---

## 4) 多模态数据（视频+音频）获取方法

### 方法 A：`yt-dlp`（主方案）

- **能力**：
  - 下载分离的视频流与音频流，并可通过 `ffmpeg` 合并
  - 支持格式选择（清晰度/编码）与输出模板，便于工程化落盘与归档
- **优点**：通用、可扩展到多平台；命令行稳定，便于集成到任务队列  
- **缺点**：B 站在高画质、字幕等方面可能需要 cookie；遇到风控时需要回退策略  
- **适用场景**：作为统一的 `MediaDownloader` 实现

参考项目：`yt-dlp/yt-dlp`（含 Bilibili extractor）  
- https://github.com/yt-dlp/yt-dlp\n+
### 方法 B：`BBDown`（备用回退）

- **优点**：B 站专用体验更好（登录、清晰度概念、字幕/弹幕周边资产支持更直接）  
- **缺点**：跨平台通用性弱于 `yt-dlp`  
- **适用场景**：当 `yt-dlp` 失败（清晰度受限/下载失败/分 P 异常）时做回退下载器

参考项目：`nilaoda/BBDown`  
- https://github.com/nilaoda/BBDown\n+
### 方法 C：网页解析/抓包直连流（兜底，不建议主线）

- **优点**：控制力最强，可做逆向验证  
- **缺点**：维护成本最高、最脆弱、风控风险最大  
- **适用场景**：研究性质兜底；不建议用于生产级持续采集

---

## 5) 第一版推荐组合（与你的当前决策一致）

- **样本池**：榜单/热门冷启动 + 分区筛选 + 作者投稿扩展  
- **时间序列**：
  - 全量：日更统计快照（面板数据）
  - 重点样本：2 小时统计快照 + 前 n 条评论快照（明确排序与快照定义）
- **多模态**：`yt-dlp` 主下载器 + `BBDown` 回退，资产进对象存储，只存引用到结构化表

---

## 6) 接口分层与推荐实现路线

- **底层接口（按数据类型）**：`crawl_video_meta`、`crawl_stat_snapshot`、`crawl_latest_comments`、`crawl_media_assets`，分别对应元数据、面板统计、评论快照、多模态资产。
- **工程入口接口（按使用场景）**：
  - 单样本全流程：`crawl_full_video_bundle(bvid, ...)`
  - 大文件流式存储：`stream_media_to_store(bvid, strategy)`
  - 列表批量：`crawl_bvid_list_from_csv(csv_path, ...)`
- **前端/调度层**：
  - Streamlit 应用 Bilibili Video Data Crawler：面向人工交互与小规模试验；
  - 定时任务/队列：面向持续面板与大规模批量。

> 总体原则：**发现/建池 → 采集 → 存储 → 指标/特征** 解耦，入口接口只负责编排，不绑定具体分析方法。
